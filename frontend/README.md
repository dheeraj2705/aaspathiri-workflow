# Hospital Workflow Automation Portal (Frontend)

This is a minimal static UI scaffold so each team can build their module in isolation and integrate later.

## Files
- `index.html` – base layout and placeholders
- `styles.css` – minimal styling
- `app.js` – simple section navigation

## Run
Open `index.html` in a browser or serve the folder with any static server.
